https://ingg.dev/webpack/
https://etloveguitar.tistory.com/m/133

ES6에 와서 모듈에 대한 표준이 도입됐다고 한다.

이전 require를 쓰는 commonJS 모듈 방식은 모듈 전체를 불러와야해서 
필요한 모듈을 모두 내려받을때까지 아무것도 할 수 없다는 단점이 존재했다.

ES6부터는 모듈 내 정의된 것들중에 필요한 객체들만 불러올 수 있기 때문에 
성능과 메모리 측면에서 매우 좋아짐

ES6에서 모듈을 export하는 방식은 크게 아래 두가지로 나뉜다.

1. default export: default 키워드를 사용해 단일 객체 내보내기
2. names export: export 키워드만 사용해 명시적으로 선언하여 여러 객체 내보내기

https://github.com/getveryrichet/nodejs-study

위 예제를 통해 연습 가능
