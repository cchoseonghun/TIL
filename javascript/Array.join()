Array.join()

배열의 모든 요소를 연결해 하나의 문자열로 만든다. default 값은 쉼표(,)로 연결되고
매개변수에 따라 연결짓는 값이 달라진다.
ex) ['1', '2', '3'].join('-');  // '1-2-3'
ex) ['1', '2', '3'].join('');  // '123'

Array.length가 0이면 빈 문자열을 반환
