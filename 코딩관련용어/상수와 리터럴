상수(Constant)
javascript에서 주로 const를 이용해 상수 선언을 하는데 여기서의 const가 constant에서 따온걸 알 수 있다.
상수는 변하지 않는 변수를 의미
다만, 참조변수에 대해선 데이터를 가리키는 주소값이 변하지 않는다는 말이지
그 주소가 가리키는 데이터는 상수가 아니다.


리터럴(Literal)
변수에 넣는 변하지 않는 데이터를 의미
let a = 1;
에서 1과 같이 변하지 않는 데이터를 말한다.
