input에 문자입력하면 경고문을 띄우고 싶다.
-> watcher 쓰자! (data 감시하는 함수)

watch : { 감시할데이터(){} }

watch : {
  month(a, b){  // a: 변경 후 데이터, b: 변경 전 데이터
    // month 라는 데이터가 변할 때마다 여기있는 코드 실행됨
  }, 
}, 

이메일, 패스워드 등 
input 에 들어갈 데이터 검사할 일이 많기 때문에
Vue 전용 form validation 라이브러리 설치해서 사용하면 좋다
그러면 watcher 안써도 됨
